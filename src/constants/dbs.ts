const BASE_API_URL = "https://dragonball-api.com/api/";
const ENDPOINTS = {
    characters: "characters",
    planets: "planets",
    transformations: "transformations"
};

export const URL_ENDPOINTS_DBS = {
    characters: BASE_API_URL + ENDPOINTS.characters,
    planets: BASE_API_URL + ENDPOINTS.planets,
    transformations: BASE_API_URL + ENDPOINTS.transformations
}
